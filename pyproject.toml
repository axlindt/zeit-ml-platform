[project]
name = "zeit-churn-model"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "scikit-learn",
    "xgboost",
    "mlflow",
    "boto3",
    "joblib",
    "s3fs",
]
